#!/bin/bash
# Скрипт для настройки git фильтров защиты секретов
# Запустите один раз после клонирования репозитория: bash .git-setup-filters.sh

echo "🔧 Настройка Git фильтров для защиты секретов..."

# Настройка фильтра для секретов
git config filter.secret.clean "bash $(pwd)/.git-secret-filter.sh"
git config filter.secret.smudge cat

# Делаем скрипт исполняемым
chmod +x .git-secret-filter.sh

echo "✅ Git фильтры настроены!"
echo ""
echo "Теперь файлы с секретами будут проверяться перед коммитом."
echo "Паттерны проверки: .env, *secret*, *password*, *credentials*, *.key"
