# Security Policy

## 🔒 Безопасность проекта pdfQaGemini

### Защита конфиденциальных данных

Этот проект использует несколько уровней защиты от случайного коммита секретов:

#### 1. `.gitignore`
Исключает файлы с потенциальными секретами:
- `.env` и `.env.*`
- `*secret*`, `*password*`, `*credentials*`
- `*.key`, `*.pem`
- `local_settings.py`

#### 2. `.gitattributes`
Применяет специальные фильтры к чувствительным файлам:
- Бинарная обработка для не-текстовых файлов
- Нормализация line endings
- Фильтр `secret` для потенциально опасных файлов

#### 3. Pre-commit hooks (рекомендуется)
Автоматическая проверка перед каждым коммитом:

```bash
# Установка
pip install pre-commit
pre-commit install

# Ручная проверка
pre-commit run --all-files
```

Включает:
- `detect-secrets` - сканирование API ключей, токенов, паролей
- `detect-private-key` - обнаружение SSH/SSL ключей
- `check-added-large-files` - предотвращение больших файлов
- Проверки Python кода (black, ruff)

### Конфигурация приложения

**Используйте QSettings (рекомендуется)**

Приложение хранит настройки через `QSettings` - они автоматически сохраняются в безопасном системном хранилище:

- **Windows**: `HKEY_CURRENT_USER\Software\pdfQaGemini\Desktop`
- **Linux**: `~/.config/pdfQaGemini/Desktop.conf`
- **macOS**: `~/Library/Preferences/com.pdfQaGemini.Desktop.plist`

**НЕ используйте `.env` файлы для продакшена!**

### Обнаружили уязвимость?

Если вы обнаружили проблему безопасности:

1. **НЕ создавайте публичный issue**
2. Свяжитесь напрямую: [ваш email]
3. Опишите проблему, шаги воспроизведения, потенциальное влияние

### Что делать если секрет попал в git?

#### 1. Немедленно смените скомпрометированные ключи
```bash
# Смените API ключи в:
# - Gemini API: https://aistudio.google.com/apikey
# - Supabase: проект -> Settings -> API
# - Cloudflare R2: аккаунт -> R2 -> Manage R2 API Tokens
```

#### 2. Удалите секрет из истории git
```bash
# Используйте git-filter-repo (рекомендуется)
pip install git-filter-repo
git filter-repo --path-glob '*secret*' --invert-paths

# Или BFG Repo-Cleaner
java -jar bfg.jar --delete-files .env
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

#### 3. Force push (внимание: координируйтесь с командой!)
```bash
git push origin --force --all
git push origin --force --tags
```

#### 4. Сообщите GitHub/GitLab
Если репозиторий публичный, попросите удалить кэшированные версии.

### Checklist перед публикацией

- [ ] `.env` файлы в `.gitignore`
- [ ] Нет хардкоженных ключей в коде
- [ ] QSettings для всех конфигурационных данных
- [ ] Pre-commit hooks установлены и работают
- [ ] Документация использует плейсхолдеры (`xxx`, `example.com`)
- [ ] `git log --all --full-history` не содержит секретов
- [ ] README содержит инструкции по безопасной настройке

### Хорошие практики

✅ **Правильно:**
```python
# Чтение из QSettings
settings = QSettings("pdfQaGemini", "Desktop")
api_key = settings.value("gemini/api_key", "")

# Или переменные окружения (только для dev)
import os
api_key = os.getenv("GEMINI_API_KEY")
```

❌ **Неправильно:**
```python
# НЕ ДЕЛАЙТЕ ТАК!
API_KEY = "AIzaSyDXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

### Зависимости

Регулярно обновляйте зависимости для патчей безопасности:

```bash
# Проверка уязвимостей
pip install safety
safety check -r requirements.txt

# Обновление
pip install --upgrade google-genai supabase PySide6
```

### Дополнительные ресурсы

- [GitHub Secret Scanning](https://docs.github.com/en/code-security/secret-scanning)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Git Security Best Practices](https://docs.github.com/en/code-security/supply-chain-security/end-to-end-supply-chain/securing-your-repository)

---

**Последнее обновление:** 2026-01-03
